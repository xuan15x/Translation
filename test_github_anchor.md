# GitHub 锚点测试文件

> ⚠️ **此文件仅用于测试 GitHub 锚点生成规则**
> 测试完成后将删除

## 📋 目录

- [带变体选择符的 emoji](#-️-带变体选择符的-emoji)
- [不带变体选择符的 emoji](#-不带变体选择符的-emoji)
- [普通中文标题](#普通中文标题)
- [带空格的中文标题](#带空格的中文标题)

---

## 🛠️ 带变体选择符的 emoji

**测试目标**: 验证 `🛠️` (U+1F6E0 U+FE0F) 的锚点生成规则

**emoji 分析**:
- `🛠` = U+1F6E0 (HAMMER AND WRENCH)
- `️` = U+FE0F (VARIATION SELECTOR-16)

**预期锚点** (如果保留变体选择符): `#-️-带变体选择符的-emoji`
**预期锚点** (如果移除变体选择符): `#-带变体选择符的-emoji`

**实际测试**: 点击目录链接 `[带变体选择符的 emoji](#-️-带变体选择符的-emoji)` 看是否跳转成功

---

## 🔧 不带变体选择符的 emoji

**测试目标**: 验证 `🔧` (U+1F527) 的锚点生成规则

**emoji 分析**:
- `🔧` = U+1F527 (WRENCH) - 无变体选择符

**预期锚点**: `#-不带变体选择符的 emoji`

---

## 普通中文标题

**测试目标**: 验证纯中文标题的锚点生成

**预期锚点**: `#普通中文标题`

---

## 带空格的中文标题

**测试目标**: 验证带空格的中文标题的锚点生成

**预期锚点**: `#带空格的中文标题`

---

## 📊 测试结果记录

### 测试方法

1. 在 GitHub 上查看此文件的渲染版本
2. 点击目录中的每个链接
3. 观察是否正确跳转到对应标题
4. 查看浏览器地址栏中的实际锚点 URL

### 预期结果

| 标题 | 预期锚点 | 测试链接 |
|------|---------|---------|
| 🛠️ 带变体选择符的 emoji | `#-带变体选择符的 emoji` | `[带变体选择符的 emoji](#-带变体选择符的 emoji)` |
| 🔧 不带变体选择符的 emoji | `#-不带变体选择符的 emoji` | `[不带变体选择符的 emoji](#-不带变体选择符的 emoji)` |
| 普通中文标题 | `#普通中文标题` | `[普通中文标题](#普通中文标题)` |
| 带空格的中文标题 | `#带空格的中文标题` | `[带空格的中文标题](#带空格的中文标题)` |

### 实际结果（请在 GitHub 上验证后填写）

| 标题 | GitHub 实际生成的锚点 | 正确的 TOC 链接格式 |
|------|---------------------|-------------------|
| 🛠️ 带变体选择符的 emoji | _待填写_ | _待填写_ |
| 🔧 不带变体选择符的 emoji | _待填写_ | _待填写_ |
| 普通中文标题 | _待填写_ | _待填写_ |
| 带空格的中文标题 | _待填写_ | _待填写_ |

---

## 🔬 技术细节

### Unicode 字符编码

```python
# 🛠️ 的字符分解
emoji_with_vs = '🛠️'
print(f'字符数：{len(emoji_with_vs)}')  # 2
print(f'字符 1: U+{ord(emoji_with_vs[0]):04X}')  # U+1F6E0
print(f'字符 2: U+{ord(emoji_with_vs[1]):04X}')  # U+FE0F

# 🔧 的字符分解
emoji_without_vs = '🔧'
print(f'字符数：{len(emoji_without_vs)}')  # 1
print(f'字符 1: U+{ord(emoji_without_vs[0]):04X}')  # U+1F527
```

### GitHub 锚点生成规则（待验证）

根据 [GitHub Flavored Markdown Spec](https://github.github.com/gfm/#heading-ids):

1. 转换为小写
2. 移除标点符号（但保留 emoji？）
3. 空格转换为 `-`
4. 连续 `-` 简化为单个 `-`

**关键问题**: 变体选择符 (U+FE0F) 是否被视为"标点符号"而移除？

---

## 📝 结论

**测试完成后，根据实际结果更新以下规则**:

```python
# scripts/check_toc_links.py 中的规则
elif 0xFE00 <= code <= 0xFE0F:
    # 变体选择符，___?___（移除/保留）
    pass  # 或者 result.append(char)
```

---

**创建日期**: 2026-04-02
**测试状态**: 待验证
